# Shell scripts and the nginx entrypoint MUST use LF, or `/bin/sh` inside the
# Linux containers fails to parse them (": invalid option") when checked out on
# Windows with core.autocrlf=true.
*.sh text eol=lf
entrypoint.sh text eol=lf
Makefile text eol=lf
*.conf text eol=lf
*.template text eol=lf
